DATABASE_URL=postgres://user:password@host:5432/dbname
PORT=3000
# No admin credentials here — run scripts/upsert-admin.js to create/reset one,
# it writes a bcrypt hash straight into the `admins` table.
